# 🚀 Настройка FastAPI бэкенда

## Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 2. Создание .env файла

Скопируйте `env.example` в `.env`:

**Windows:**
```cmd
copy env.example .env
```

**Linux/Mac:**
```bash
cp env.example .env
```

### 3. Заполнение .env файла

Откройте `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=8022962647:AAEJwe0wmomcbrxG82J3v1z2zIY2PcbBS2E
TELEGRAM_CHAT_ID=760723570
```

⚠️ **Используйте ваши реальные токены!**

### 4. Запуск сервера

**Вариант 1: Через скрипт (Windows)**
```cmd
start.bat
```

**Вариант 2: Через скрипт (Linux/Mac)**
```bash
chmod +x start.sh
./start.sh
```

**Вариант 3: Вручную**
```bash
uvicorn main:app --reload --port 8001
```

### 5. Проверка работы

Откройте в браузере: `http://localhost:8001`

Должно появиться:
```json
{
  "status": "ok",
  "message": "Securizor Telegram API работает"
}
```

## 🔧 Настройка фронтенда

Фронтенд уже настроен на работу с бэкендом на `http://localhost:8001`.

Для продакшена измените в `assets/js/telegram-form.js`:

```javascript
const API_URL = 'https://ваш-домен.ru/api/send-form';
```

## 📡 API Endpoint

### POST /api/send-form

**Тело запроса:**
```json
{
  "name": "Иван Иванов",
  "contact": "+7 (999) 123-45-67",
  "comment": "Хочу узнать о ваших услугах",
  "theme": "Оставить заявку",
  "platform": "WhatsApp"
}
```

**Успешный ответ:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена в Telegram"
}
```

## 🔒 Безопасность

✅ **Токен бота теперь скрыт на сервере!**
- Токен хранится в `.env` файле
- `.env` добавлен в `.gitignore`
- Токен не передается клиенту

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'fastapi'"
```bash
pip install -r requirements.txt
```

### Ошибка: "TELEGRAM_BOT_TOKEN не найден"
- Проверьте, что файл `.env` существует
- Проверьте, что в `.env` указаны правильные значения
- Убедитесь, что нет лишних пробелов

### Ошибка: "Address already in use"
- Порт 8001 занят
- Измените порт в `start.bat`/`start.sh` или запустите:
```bash
uvicorn main:app --reload --port 8002
```
- И обновите `API_URL` во фронтенде

## 📝 Структура проекта

```
backend/
├── main.py              # FastAPI приложение
├── requirements.txt     # Зависимости Python
├── env.example          # Пример конфигурации
├── .env                 # Ваши токены (не коммитить!)
├── .gitignore          # Игнорируемые файлы
├── README.md            # Документация
├── start.bat            # Скрипт запуска (Windows)
└── start.sh             # Скрипт запуска (Linux/Mac)
```

## 🚀 Деплой на продакшен

### Вариант 1: VPS сервер

1. Загрузите код на сервер
2. Установите зависимости: `pip install -r requirements.txt`
3. Создайте `.env` файл с токенами
4. Запустите через systemd или supervisor
5. Настройте nginx как reverse proxy

### Вариант 2: Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
```

Запуск:
```bash
docker build -t securizor-backend .
docker run -p 8001:8001 --env-file .env securizor-backend
```

### Вариант 3: Cloud платформы

- **Heroku**: Добавьте переменные окружения в настройках
- **Railway**: Используйте секреты
- **Render**: Настройте Environment Variables
